<TrendsScreen@MDBoxLayout>:
    orientation: 'vertical'
    size_hint: .8, .8
    spacing: dp(5)
    MDBoxLayout:
        spacing: dp(5)
        size_hint: 1, .1
        pos_hint: {'x': .1, 'y': .9}
        MDFillRoundFlatButton:
            id: trend_start_date_button
            text: "Start Date:"
            md_bg_color: app.theme_cls.primary_color
            on_press: app.show_date_picker(*args)
        MDFillRoundFlatButton:
            text: "End Date:"
            id: trend_end_date_button
            md_bg_color: app.theme_cls.primary_color
            on_release: app.show_date_picker(*args)
        MDFillRoundFlatIconButton:
            text: "Create"
            icon: 'chart-box'
            md_bg_color: app.theme_cls.primary_color
            on_release: app.generate_trend(*args)
    MDBoxLayout:
        id: trend_chart_layout
    MDBoxLayout:
        id: trend_pie_chart_layout

